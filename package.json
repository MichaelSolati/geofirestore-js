{
  "name": "geofirestore-js",
  "description": "Monorepo for geofirestore",
  "version": "5.0.0",
  "scripts": {
    "bootstrap": "lerna bootstrap --force-local --include-dependencies --hoist --nohoist=gts --nohoist=@types/*",
    "build": "lerna run build",
    "lint": "lerna run lint",
    "test": "lerna run test",
    "coverage": "lerna run coverage"
  },
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/MichaelSolati/geofirestore-js.git"
  },
  "bugs": {
    "url": "https://github.com/MichaelSolati/geofirestore-js/issues"
  },
  "readme": "README.md",
  "license": "MIT",
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}